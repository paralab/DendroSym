/**
 * @file {{ project_name }}_constraints.h
 * @brief Evolution constraint enforcement for the {{ project_name }} solver.
 *
 * AUTO-GENERATED by dendrosym project generator. Do not edit by hand.
 *
 * This enforces algebraic constraints after each RK substep:
 * - Metric determinant = 1
 * - Traceless condition on At (or similar)
 * - Positive floor on chi, alpha, etc.
 */

#ifndef {{ project_upper }}_CONSTRAINTS_H
#define {{ project_upper }}_CONSTRAINTS_H

#include "grDef.h"
#include "parameters.h"

#include <cmath>
#include <iostream>

namespace {{ namespace }} {

inline void enforce_{{ project_name }}_constraints(double **uzipVars,
                                     const unsigned int node) {
{% if evolution_constraint_enforcement is defined and evolution_constraint_enforcement %}
    {{ evolution_constraint_enforcement }}
{% else %}
    // No evolution constraints configured.
    // To add constraints, use dendroConfigs.add_evolution_constraint()
    // in the Python equation config, or add them manually here.
{% endif %}
}

}  // namespace {{ namespace }}

#endif  // {{ project_upper }}_CONSTRAINTS_H
